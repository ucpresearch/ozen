PyQt6>=6.6.0
pyqtgraph>=0.13.0
praatfan @ git+https://github.com/ucpresearch/praatfan-core-clean.git

# Optional backends (user installs separately):
# praat-parselmouth>=0.4.0  # GPL - for parselmouth backend
sounddevice>=0.4.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.11.0
pytest>=7.0.0
PyYAML>=6.0  # For config file support (optional, falls back to JSON)
